{
  "openapi" : "3.0.1",
  "info" : {
    "title" : "Plotted API",
    "description" : "Platform-neutral streaming decision and subscription optimisation platform. Title metadata and regional availability originate from TMDB and JustWatch; see /legal/data-sources for attribution and refresh cadence.",
    "license" : {
      "name" : "AGPL-3.0-or-later"
    },
    "version" : "v1"
  },
  "servers" : [ {
    "url" : "/",
    "description" : "This deployment"
  } ],
  "security" : [ {
    "bearerAuth" : [ ]
  } ],
  "paths" : {
    "/api/v1/watchlist/items" : {
      "post" : {
        "tags" : [ "watchlist-controller" ],
        "summary" : "Add a title to the watchlist",
        "description" : "Idempotent: adding a title that is already on the list returns the existing entry rather than failing, because adding something twice is a slip rather than an error.",
        "operationId" : "add",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/AddWatchlistItemRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/WatchlistItemResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/watchlist/blocked" : {
      "get" : {
        "tags" : [ "watchlist-controller" ],
        "summary" : "Titles the signed-in user has asked never to be recommended",
        "description" : "Most recently blocked first. This list is what makes blocking reversible: a preference you can set and never see again is a one-way door.",
        "operationId" : "listBlocked",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BlockedTitlesResponse"
                }
              }
            }
          }
        }
      },
      "post" : {
        "tags" : [ "watchlist-controller" ],
        "summary" : "Block a title from recommendations",
        "description" : "Suppresses the title in Tonight Mode and in the subscription optimiser. It does NOT hide the title from catalogue search, and it does not remove it from the watchlist -- a blocked watchlist entry is returned marked rather than deleted, so unblocking restores the priority and notes intact. Idempotent: blocking twice returns the original block, reason and timestamp included.",
        "operationId" : "block",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/BlockTitleRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/BlockedTitleResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tonight/{requestId}/accept" : {
      "post" : {
        "tags" : [ "tonight-controller" ],
        "summary" : "Record that you are watching one of tonight's picks",
        "description" : "Attaches the choice to the exact item that was offered, which is what makes it usable for evaluation: the position and the propensity travel with it. Accepting a title that was not among the picks, or someone else's recommendation, is a 404. Only the first acceptance counts — the second click is not a second decision.",
        "operationId" : "accept",
        "parameters" : [ {
          "name" : "requestId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/AcceptPickRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      }
    },
    "/api/v1/titles" : {
      "post" : {
        "tags" : [ "catalogue-controller" ],
        "summary" : "Ingest a title from TMDB",
        "description" : "Idempotent: ingesting the same title again refreshes it in place and returns 200 rather than creating a duplicate. Availability is fetched separately, after this returns, so a provider outage cannot fail the ingest.",
        "operationId" : "ingest",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/IngestTitleRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TitleResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/subscriptions" : {
      "get" : {
        "tags" : [ "subscription-controller" ],
        "summary" : "What the signed-in user pays for",
        "description" : "monthlyTotal counts active and trial subscriptions only. Cancelled and lapsed rows are kept and returned, because what someone used to pay for is part of the record, but they cost nothing now and including them would overstate the bill.",
        "operationId" : "list_1",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SubscriptionListResponse"
                }
              }
            }
          }
        }
      },
      "post" : {
        "tags" : [ "subscription-controller" ],
        "summary" : "Record a subscription",
        "operationId" : "add_1",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/CreateSubscriptionRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SubscriptionResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/pilot/answers" : {
      "post" : {
        "tags" : [ "pilot-controller" ],
        "summary" : "Answer a question, or skip it",
        "description" : "Omit chosenTitleId to skip. A skipped pair is recorded so it is not asked again and is excluded from the fit: a forced choice between two unseen titles is a coin flip, and recording one as a preference is worse than a shorter questionnaire. Idempotent on the pair — answering the same comparison twice keeps the first answer, because the fit counts rows and a duplicate would weight one opinion twice. Returns the state that follows, so a client needs one request per answer rather than two.",
        "operationId" : "answer",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/PilotAnswerRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PilotStateResponse"
                }
              }
            }
          }
        }
      },
      "delete" : {
        "tags" : [ "pilot-controller" ],
        "summary" : "Discard every answer and start again",
        "description" : "Deletes the comparisons rather than marking them superseded. Somebody who wants to redo the questionnaire is saying the old answers were wrong, and keeping them to fit against later would be disagreeing with them.",
        "operationId" : "reset",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      }
    },
    "/api/v1/demo/session" : {
      "post" : {
        "tags" : [ "demo-controller" ],
        "summary" : "Start a demo session without signing up",
        "description" : "Creates a throwaway account with a watchlist and subscriptions built from the seeded catalogue, and returns a normal session for it. The account expires and is swept. Returns 404 when demo mode is not enabled on this deployment, and 429 when the demo is at its account ceiling — this endpoint writes and is unauthenticated, so it refuses rather than degrading.",
        "operationId" : "start",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/DemoSessionResponse"
                }
              }
            }
          }
        },
        "security" : [ ]
      }
    },
    "/api/v1/auth/register" : {
      "post" : {
        "tags" : [ "auth-controller" ],
        "summary" : "Create an account and start a session",
        "operationId" : "register",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RegisterRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SessionResponse"
                }
              }
            }
          }
        },
        "security" : [ ]
      }
    },
    "/api/v1/auth/refresh" : {
      "post" : {
        "tags" : [ "auth-controller" ],
        "summary" : "Rotate the refresh token and issue a new access token",
        "description" : "Reads the HttpOnly refresh cookie. Presenting an already-spent token revokes the entire token family, so a leaked token cannot be used quietly alongside the legitimate session.",
        "operationId" : "refresh",
        "parameters" : [ {
          "name" : "plotted_refresh",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SessionResponse"
                }
              }
            }
          }
        },
        "security" : [ ]
      }
    },
    "/api/v1/auth/logout" : {
      "post" : {
        "tags" : [ "auth-controller" ],
        "summary" : "End the session and revoke its refresh-token family",
        "operationId" : "logout",
        "parameters" : [ {
          "name" : "plotted_refresh",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      }
    },
    "/api/v1/auth/login" : {
      "post" : {
        "tags" : [ "auth-controller" ],
        "summary" : "Exchange credentials for a session",
        "operationId" : "login",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/LoginRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SessionResponse"
                }
              }
            }
          }
        },
        "security" : [ ]
      }
    },
    "/api/v1/watchlist/items/{itemId}" : {
      "delete" : {
        "tags" : [ "watchlist-controller" ],
        "summary" : "Remove a title from the watchlist",
        "operationId" : "remove",
        "parameters" : [ {
          "name" : "itemId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      },
      "patch" : {
        "tags" : [ "watchlist-controller" ],
        "summary" : "Update priority, status or intent for a watchlist entry",
        "description" : "Partial update; omitted fields are unchanged.",
        "operationId" : "update",
        "parameters" : [ {
          "name" : "itemId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateWatchlistItemRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/WatchlistItemResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/users/me/settings" : {
      "get" : {
        "tags" : [ "user-controller" ],
        "summary" : "Recommendation and budget defaults for the signed-in account",
        "operationId" : "settings",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserSettingsResponse"
                }
              }
            }
          }
        }
      },
      "patch" : {
        "tags" : [ "user-controller" ],
        "summary" : "Update recommendation and budget defaults",
        "description" : "Partial update; omitted fields are unchanged. weeklyViewingMinutesOverride replaces the trailing eight-week estimate that drives completion feasibility and cancellation advice.",
        "operationId" : "updateSettings",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateUserSettingsRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserSettingsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/subscriptions/{subscriptionId}" : {
      "delete" : {
        "tags" : [ "subscription-controller" ],
        "summary" : "Forget a subscription entirely",
        "description" : "Deletes the record. To mark something as no longer paid for while keeping its history, PATCH the status to cancelled instead.",
        "operationId" : "remove_1",
        "parameters" : [ {
          "name" : "subscriptionId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      },
      "patch" : {
        "tags" : [ "subscription-controller" ],
        "summary" : "Update a subscription",
        "description" : "Partial update; omitted fields are unchanged.",
        "operationId" : "update_1",
        "parameters" : [ {
          "name" : "subscriptionId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateSubscriptionRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SubscriptionResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/alerts/{alertId}" : {
      "patch" : {
        "tags" : [ "alert-controller" ],
        "summary" : "Mark an alert read or dismissed",
        "description" : "Dismissing also silences repeats about the same title for a while: dismissing is an answer, and re-sending would be the behaviour that gets notifications turned off.",
        "operationId" : "update_2",
        "parameters" : [ {
          "name" : "alertId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UpdateAlertRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      }
    },
    "/api/v1/watchlist" : {
      "get" : {
        "tags" : [ "watchlist-controller" ],
        "summary" : "The signed-in user's watchlist",
        "description" : "Ordered by priority, highest first, then by when each title was added.",
        "operationId" : "list",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/WatchlistResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/watchlist/coverage" : {
      "get" : {
        "tags" : [ "coverage-controller" ],
        "summary" : "Which service covers the most of the watchlist",
        "description" : "Shares are weighted by priority rather than counted, so one title someone urgently wants outranks several they are lukewarm about. Only outstanding items count: something already watched says nothing about which service to pay for next month. Titles whose availability has never been checked are excluded and reported in unknownTitles, because scoring them as uncovered would penalise every service in proportion to how stale Plotted's own data is.",
        "operationId" : "coverage",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CoverageResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/users/me" : {
      "get" : {
        "tags" : [ "user-controller" ],
        "summary" : "The signed-in account",
        "operationId" : "me",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UserResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tonight" : {
      "get" : {
        "tags" : [ "tonight-controller" ],
        "summary" : "What to watch tonight",
        "description" : "One pick and two backups, each with the reasons that actually produced its score. Returns 200 with an empty picks list and a diagnosis when nothing fits: the constraints were the request, so relaxing them silently to produce something would be answering a different question.",
        "operationId" : "tonight",
        "parameters" : [ {
          "name" : "availableMinutes",
          "in" : "query",
          "description" : "Minutes available. Omit for no time limit — which is not the same as zero.",
          "required" : false,
          "schema" : {
            "type" : "integer",
            "format" : "int32"
          }
        }, {
          "name" : "accessPolicy",
          "in" : "query",
          "description" : "One of active_subscriptions_only, include_free, any_subscription.",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TonightResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/titles/{titleId}" : {
      "get" : {
        "tags" : [ "catalogue-controller" ],
        "summary" : "A single ingested title",
        "operationId" : "byId",
        "parameters" : [ {
          "name" : "titleId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TitleResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/titles/{titleId}/availability" : {
      "get" : {
        "tags" : [ "availability-controller" ],
        "summary" : "Where a title can be watched",
        "description" : "Always answers, even when the data is old. Staleness is reported rather than hidden: the degraded behaviour Plotted promises is cached availability with a visible timestamp, not a pretence of knowing nothing.",
        "operationId" : "byTitle",
        "parameters" : [ {
          "name" : "titleId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/AvailabilityResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/titles/search" : {
      "get" : {
        "tags" : [ "catalogue-controller" ],
        "summary" : "Search the ingested catalogue",
        "description" : "Full-text and trigram matching, so a typo still finds the title.",
        "operationId" : "search",
        "parameters" : [ {
          "name" : "query",
          "in" : "query",
          "required" : true,
          "schema" : {
            "maxLength" : 200,
            "minLength" : 1,
            "type" : "string"
          }
        }, {
          "name" : "mediaType",
          "in" : "query",
          "required" : false,
          "schema" : {
            "type" : "string",
            "enum" : [ "MOVIE", "SERIES" ]
          }
        }, {
          "name" : "limit",
          "in" : "query",
          "required" : false,
          "schema" : {
            "type" : "integer",
            "format" : "int32",
            "default" : 20
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/TitleResponse"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/titles/discover" : {
      "get" : {
        "tags" : [ "catalogue-controller" ],
        "summary" : "Search TMDB for titles Plotted may not have yet",
        "description" : "Degrades to an empty list when TMDB is unreachable rather than failing the request: an empty search box is a better outcome than an error page.",
        "operationId" : "discover",
        "parameters" : [ {
          "name" : "query",
          "in" : "query",
          "required" : true,
          "schema" : {
            "maxLength" : 200,
            "minLength" : 1,
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/DiscoverResultResponse"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/providers" : {
      "get" : {
        "tags" : [ "provider-controller" ],
        "summary" : "Services a user could be subscribed to",
        "description" : "Filtered to providers that bill on a recurring basis, so a rental storefront does not appear on a screen asking what someone subscribes to. Carries no pricing: Plotted ships none, because a price it invented would reach the optimiser.",
        "operationId" : "list_2",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ProviderListResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/plan" : {
      "get" : {
        "tags" : [ "plan-controller" ],
        "summary" : "Which subscriptions to keep, month by month",
        "description" : "A constraint-optimised subscription plan over the next few months, with the objective recomputed exactly by an independent checker rather than read out of the solver. Returns 200 with an empty plan and a diagnosis when the limits cannot all be met: the limits were the request, so quietly relaxing one would be answering a different question. Titles that are free to watch, never checked, or only on services with no established price are excluded from the model and reported separately.",
        "operationId" : "plan",
        "parameters" : [ {
          "name" : "horizonMonths",
          "in" : "query",
          "description" : "Months to plan over, 1 to 12. Defaults to 6.",
          "required" : false,
          "schema" : {
            "type" : "integer",
            "format" : "int32"
          }
        }, {
          "name" : "maximumMonthlyCents",
          "in" : "query",
          "description" : "Most you are willing to pay in any one month, in cents.",
          "required" : false,
          "schema" : {
            "type" : "integer",
            "format" : "int64"
          }
        }, {
          "name" : "maximumActiveServices",
          "in" : "query",
          "description" : "Most services to hold at once.",
          "required" : false,
          "schema" : {
            "type" : "integer",
            "format" : "int32"
          }
        }, {
          "name" : "maximumMonthlySwitches",
          "in" : "query",
          "description" : "Most signups plus cancellations in any one month.",
          "required" : false,
          "schema" : {
            "type" : "integer",
            "format" : "int32"
          }
        }, {
          "name" : "coverageWeight",
          "in" : "query",
          "description" : "How much coverage matters, 0 to 1. Cost and churn take the remainder in a 3.5:1 ratio.",
          "required" : false,
          "schema" : {
            "type" : "number",
            "format" : "double"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PlanResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/pilot" : {
      "get" : {
        "tags" : [ "pilot-controller" ],
        "summary" : "The next question, and how far through the user is",
        "description" : "The ladder is deterministic given the catalogue, so abandoning the flow and returning continues it rather than restarting on different questions.",
        "operationId" : "next",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PilotStateResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/pilot/profile" : {
      "get" : {
        "tags" : [ "pilot-controller" ],
        "summary" : "The fitted taste profile",
        "description" : "204 when nothing has been answered yet. A profile fitted from no answers is the population's rather than yours, and returning it would report the prior as a finding.",
        "operationId" : "profile",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/PreferenceProfileResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/analytics/end-credits" : {
      "get" : {
        "tags" : [ "end-credits-controller" ],
        "summary" : "Whether Plotted actually helped",
        "description" : "Decision latency and accepted-and-completed rate. Both are null until there is enough to compute them from, rather than defaulting to a flattering zero, and both report what was excluded alongside what was counted.",
        "operationId" : "endCredits",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/EndCreditsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/alerts" : {
      "get" : {
        "tags" : [ "alert-controller" ],
        "summary" : "Unread alerts for the signed-in user",
        "description" : "Newest first. Read and dismissed alerts are not returned.",
        "operationId" : "list_3",
        "responses" : {
          "200" : {
            "description" : "OK",
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/AlertsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/watchlist/blocked/{titleId}" : {
      "delete" : {
        "tags" : [ "watchlist-controller" ],
        "summary" : "Stop blocking a title",
        "description" : "404 when the title was not blocked, so a client can tell an undo from a no-op.",
        "operationId" : "unblock",
        "parameters" : [ {
          "name" : "titleId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        }
      }
    }
  },
  "components" : {
    "schemas" : {
      "AddWatchlistItemRequest" : {
        "required" : [ "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "priority" : {
            "maximum" : 5,
            "minimum" : 1,
            "type" : "integer",
            "description" : "1 is the highest priority and 5 the lowest. Defaults to 3.",
            "format" : "int32"
          },
          "desiredByDate" : {
            "type" : "string",
            "format" : "date"
          },
          "notes" : {
            "maxLength" : 2000,
            "minLength" : 0,
            "type" : "string"
          }
        }
      },
      "WatchlistItemResponse" : {
        "required" : [ "addedAt", "blocked", "id", "priority", "status", "titleId" ],
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "priority" : {
            "type" : "integer",
            "description" : "1 is the highest priority and 5 the lowest.",
            "format" : "int32"
          },
          "status" : {
            "type" : "string"
          },
          "addedAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "completedAt" : {
            "type" : "string",
            "description" : "When this item became completed. Null unless status is completed, and also null on a completed item whose transition predates this field -- unknown, rather than zero.",
            "format" : "date-time"
          },
          "desiredByDate" : {
            "type" : "string",
            "format" : "date"
          },
          "notes" : {
            "type" : "string"
          },
          "blocked" : {
            "type" : "boolean",
            "description" : "True when this title is also blocked. It keeps its place on the list rather than being deleted, but neither recommender will offer it while the block stands."
          },
          "title" : {
            "$ref" : "#/components/schemas/WatchlistTitleResponse"
          }
        },
        "description" : "A title on the signed-in user's watchlist."
      },
      "WatchlistTitleResponse" : {
        "required" : [ "mediaType", "name" ],
        "type" : "object",
        "properties" : {
          "name" : {
            "type" : "string"
          },
          "mediaType" : {
            "type" : "string"
          },
          "releaseYear" : {
            "type" : "integer",
            "format" : "int32"
          },
          "posterUrl" : {
            "type" : "string"
          },
          "watchMinutes" : {
            "type" : "integer",
            "description" : "Runtime for a film, total episode runtime for a series. Null when unknown.",
            "format" : "int32"
          }
        },
        "description" : "Null when the title has been removed from the catalogue since it was added."
      },
      "BlockTitleRequest" : {
        "required" : [ "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "reason" : {
            "maxLength" : 64,
            "minLength" : 0,
            "type" : "string",
            "description" : "Optional free-text note. Stored, shown back, never interpreted."
          }
        }
      },
      "BlockedTitleResponse" : {
        "required" : [ "blockedAt", "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "reason" : {
            "type" : "string",
            "description" : "Why they blocked it, if they said. Free text, never interpreted."
          },
          "blockedAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "title" : {
            "$ref" : "#/components/schemas/WatchlistTitleResponse"
          }
        },
        "description" : "A title the user has asked never to be recommended. Blocking suppresses Tonight Mode and the subscription optimiser; it does not hide the title from catalogue search."
      },
      "AcceptPickRequest" : {
        "required" : [ "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          }
        },
        "description" : "Which of tonight's picks you are actually watching."
      },
      "IngestTitleRequest" : {
        "required" : [ "mediaType", "tmdbId" ],
        "type" : "object",
        "properties" : {
          "mediaType" : {
            "type" : "string",
            "enum" : [ "MOVIE", "SERIES" ]
          },
          "tmdbId" : {
            "minimum" : 1,
            "type" : "integer",
            "format" : "int32"
          }
        },
        "description" : "Ingest a title from TMDB into the Plotted catalogue."
      },
      "TitleResponse" : {
        "required" : [ "id", "mediaType", "metadataStatus", "name" ],
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "mediaType" : {
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "originalName" : {
            "type" : "string"
          },
          "overview" : {
            "type" : "string"
          },
          "releaseDate" : {
            "type" : "string",
            "format" : "date"
          },
          "posterUrl" : {
            "type" : "string"
          },
          "communityRating" : {
            "type" : "number"
          },
          "watchMinutes" : {
            "type" : "integer",
            "description" : "How long this takes to watch: a film's runtime, or a series' estimated total. Null means Plotted does not know, and a time-constrained recommendation must exclude it.",
            "format" : "int32"
          },
          "episodeCount" : {
            "type" : "integer",
            "format" : "int32"
          },
          "metadataStatus" : {
            "type" : "string",
            "description" : "stub, partial, complete or failed. Anything but complete is missing something."
          }
        },
        "description" : "A title Plotted has already ingested."
      },
      "CreateSubscriptionRequest" : {
        "required" : [ "autoRenews", "cannotCancel", "price", "providerId" ],
        "type" : "object",
        "properties" : {
          "providerId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "planName" : {
            "maxLength" : 120,
            "minLength" : 0,
            "type" : "string",
            "description" : "The plan as the user knows it, for example 'Standard with ads'. Defaults to 'Standard'."
          },
          "billingPeriod" : {
            "type" : "string",
            "description" : "One of monthly, quarterly, annual.",
            "enum" : [ "monthly", "quarterly", "annual" ]
          },
          "price" : {
            "minimum" : 0.0,
            "exclusiveMinimum" : false,
            "type" : "number"
          },
          "currency" : {
            "maxLength" : 3,
            "minLength" : 3,
            "type" : "string"
          },
          "status" : {
            "type" : "string",
            "description" : "One of active, paused, cancelled, trial, lapsed. Defaults to active.",
            "enum" : [ "active", "paused", "cancelled", "trial", "lapsed" ]
          },
          "startedOn" : {
            "type" : "string",
            "format" : "date"
          },
          "renewsOn" : {
            "type" : "string",
            "format" : "date"
          },
          "commitmentEndsOn" : {
            "type" : "string",
            "format" : "date"
          },
          "autoRenews" : {
            "type" : "boolean"
          },
          "cannotCancel" : {
            "type" : "boolean"
          },
          "notes" : {
            "maxLength" : 2000,
            "minLength" : 0,
            "type" : "string"
          }
        },
        "description" : "Records a subscription the user pays for. Price is supplied by the user rather than looked up: Plotted deliberately ships no pricing data, because a figure it invented would flow straight into the cancellation optimiser's objective."
      },
      "SubscriptionResponse" : {
        "required" : [ "autoRenews", "billingPeriod", "cannotCancel", "currency", "id", "monthlyCost", "planName", "price", "providerId", "providerName", "providerSlug", "startedOn", "status" ],
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "providerId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "providerName" : {
            "type" : "string"
          },
          "providerSlug" : {
            "type" : "string"
          },
          "providerLogoUrl" : {
            "type" : "string"
          },
          "planName" : {
            "type" : "string"
          },
          "billingPeriod" : {
            "type" : "string"
          },
          "price" : {
            "type" : "number",
            "description" : "What the user told us they pay, per billing period."
          },
          "monthlyCost" : {
            "type" : "number",
            "description" : "The same figure expressed per month, so plans on different cycles compare."
          },
          "currency" : {
            "type" : "string"
          },
          "status" : {
            "type" : "string"
          },
          "startedOn" : {
            "type" : "string",
            "format" : "date"
          },
          "renewsOn" : {
            "type" : "string",
            "format" : "date"
          },
          "autoRenews" : {
            "type" : "boolean"
          },
          "cannotCancel" : {
            "type" : "boolean",
            "description" : "Inside a commitment. Phase 5 treats this as a hard constraint, never a cost."
          },
          "commitmentEndsOn" : {
            "type" : "string",
            "format" : "date"
          },
          "notes" : {
            "type" : "string"
          }
        }
      },
      "PilotAnswerRequest" : {
        "required" : [ "leftTitleId", "rightTitleId" ],
        "type" : "object",
        "properties" : {
          "leftTitleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "rightTitleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "chosenTitleId" : {
            "type" : "string",
            "description" : "The title picked. Null or absent means skip.",
            "format" : "uuid"
          }
        },
        "description" : "An answer. Omit `chosenTitleId` to skip: a forced choice between two titles somebody has not seen is a coin flip, and it is recorded as declined rather than as a preference."
      },
      "PilotOptionResponse" : {
        "required" : [ "mediaType", "name", "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "mediaType" : {
            "type" : "string"
          },
          "releaseYear" : {
            "type" : "integer",
            "format" : "int32"
          },
          "posterUrl" : {
            "type" : "string"
          }
        }
      },
      "PilotQuestionResponse" : {
        "required" : [ "axis", "axisLabel", "left", "position", "right" ],
        "type" : "object",
        "properties" : {
          "left" : {
            "$ref" : "#/components/schemas/PilotOptionResponse"
          },
          "right" : {
            "$ref" : "#/components/schemas/PilotOptionResponse"
          },
          "axis" : {
            "type" : "string",
            "description" : "The axis this pair was chosen to isolate. Shown for transparency, not required to answer."
          },
          "axisLabel" : {
            "type" : "string"
          },
          "position" : {
            "type" : "integer",
            "format" : "int32"
          }
        }
      },
      "PilotStateResponse" : {
        "required" : [ "answered", "complete", "exhausted", "skipped", "total" ],
        "type" : "object",
        "properties" : {
          "question" : {
            "$ref" : "#/components/schemas/PilotQuestionResponse"
          },
          "answered" : {
            "type" : "integer",
            "description" : "Answers that count as evidence. Skipped questions are not among them.",
            "format" : "int32"
          },
          "skipped" : {
            "type" : "integer",
            "description" : "Questions declined. Recorded so they are not asked again, and excluded from the fit.",
            "format" : "int32"
          },
          "total" : {
            "type" : "integer",
            "format" : "int32"
          },
          "complete" : {
            "type" : "boolean"
          },
          "exhausted" : {
            "type" : "boolean",
            "description" : "True when the ladder ran out of usable pairs before reaching `total`. The catalogue is too small or too uniform, which is a different problem from a finished questionnaire."
          }
        },
        "description" : "Where the signed-in user is in the questionnaire. `question` is null when there is nothing left to ask, and `exhausted` says which kind of nothing: a finished questionnaire, or a catalogue too thin to supply another contrasting pair."
      },
      "DemoSessionResponse" : {
        "required" : [ "accessToken", "catalogueIsEmpty", "displayName", "expiresAt", "subscriptions", "tokenType", "userId", "watchlistSize" ],
        "type" : "object",
        "properties" : {
          "accessToken" : {
            "type" : "string"
          },
          "tokenType" : {
            "type" : "string"
          },
          "expiresAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "userId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "displayName" : {
            "type" : "string"
          },
          "watchlistSize" : {
            "type" : "integer",
            "description" : "How many titles the demo persona's list was built with.",
            "format" : "int32"
          },
          "subscriptions" : {
            "type" : "array",
            "description" : "Services the persona pays for, chosen from what actually covers their list.",
            "items" : {
              "type" : "string",
              "description" : "Services the persona pays for, chosen from what actually covers their list."
            }
          },
          "catalogueIsEmpty" : {
            "type" : "boolean",
            "description" : "True when the catalogue has not been seeded, so the demo has no titles to work with. Reported rather than hidden: two empty screens caused by a missing catalogue look exactly like two broken features, and the difference matters to whoever is looking."
          }
        },
        "description" : "A demo session. Shaped like the normal session response so the client's sign-in path does not need a second branch, with the demo-specific facts alongside it."
      },
      "RegisterRequest" : {
        "required" : [ "displayName", "email", "password" ],
        "type" : "object",
        "properties" : {
          "email" : {
            "maxLength" : 320,
            "minLength" : 0,
            "type" : "string"
          },
          "password" : {
            "maxLength" : 200,
            "minLength" : 12,
            "type" : "string"
          },
          "displayName" : {
            "maxLength" : 120,
            "minLength" : 0,
            "type" : "string"
          },
          "regionCode" : {
            "pattern" : "^[A-Za-z]{2}$",
            "type" : "string"
          },
          "timezone" : {
            "maxLength" : 64,
            "minLength" : 0,
            "type" : "string"
          }
        },
        "description" : "Create an account. The response also establishes a session."
      },
      "SessionResponse" : {
        "required" : [ "accessToken", "expiresAt", "expiresIn", "tokenType", "user" ],
        "type" : "object",
        "properties" : {
          "accessToken" : {
            "type" : "string"
          },
          "tokenType" : {
            "type" : "string"
          },
          "expiresIn" : {
            "type" : "integer",
            "format" : "int64"
          },
          "expiresAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "user" : {
            "$ref" : "#/components/schemas/UserResponse"
          }
        },
        "description" : "The access token is short-lived and belongs in memory. The refresh token is returned as an HttpOnly cookie and never appears in this body."
      },
      "UserResponse" : {
        "required" : [ "createdAt", "displayName", "email", "id", "isDemo", "onboardingStatus", "preferredCurrency", "regionCode", "timezone" ],
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "email" : {
            "type" : "string"
          },
          "displayName" : {
            "type" : "string"
          },
          "regionCode" : {
            "type" : "string"
          },
          "timezone" : {
            "type" : "string"
          },
          "preferredCurrency" : {
            "type" : "string"
          },
          "onboardingStatus" : {
            "type" : "string"
          },
          "createdAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "isDemo" : {
            "type" : "boolean",
            "description" : "True when this account's watchlist, subscriptions, taste profile and viewing history were generated for the demo rather than recorded from use. The interface says so where it would otherwise imply those figures were measured."
          }
        }
      },
      "LoginRequest" : {
        "required" : [ "email", "password" ],
        "type" : "object",
        "properties" : {
          "email" : {
            "type" : "string"
          },
          "password" : {
            "type" : "string"
          }
        }
      },
      "UpdateWatchlistItemRequest" : {
        "required" : [ "clearDesiredByDate", "clearNotes" ],
        "type" : "object",
        "properties" : {
          "priority" : {
            "maximum" : 5,
            "minimum" : 1,
            "type" : "integer",
            "format" : "int32"
          },
          "status" : {
            "type" : "string",
            "description" : "One of pending, in_progress, completed, abandoned, unavailable.",
            "enum" : [ "pending", "in_progress", "completed", "abandoned", "unavailable" ]
          },
          "desiredByDate" : {
            "type" : "string",
            "format" : "date"
          },
          "clearDesiredByDate" : {
            "type" : "boolean",
            "description" : "Set true to remove an existing desiredByDate; JSON null alone cannot express this."
          },
          "notes" : {
            "maxLength" : 2000,
            "minLength" : 0,
            "type" : "string"
          },
          "clearNotes" : {
            "type" : "boolean",
            "description" : "Set true to remove existing notes."
          }
        }
      },
      "UpdateUserSettingsRequest" : {
        "type" : "object",
        "properties" : {
          "maximumMonthlyBudget" : {
            "maximum" : 10000.0,
            "exclusiveMaximum" : false,
            "minimum" : 0.0,
            "exclusiveMinimum" : false,
            "type" : "number"
          },
          "maximumActiveServices" : {
            "maximum" : 20,
            "minimum" : 0,
            "type" : "integer",
            "format" : "int32"
          },
          "maximumMonthlySwitches" : {
            "maximum" : 20,
            "minimum" : 0,
            "type" : "integer",
            "format" : "int32"
          },
          "defaultAvailableMinutes" : {
            "maximum" : 1440,
            "minimum" : 5,
            "type" : "integer",
            "format" : "int32"
          },
          "defaultAccessPolicy" : {
            "type" : "string",
            "enum" : [ "active_subscriptions_only", "include_free", "all_access" ]
          },
          "defaultNoveltyPreference" : {
            "maximum" : 1.0,
            "exclusiveMaximum" : false,
            "minimum" : 0.0,
            "exclusiveMinimum" : false,
            "type" : "number"
          },
          "defaultCommitmentPreference" : {
            "type" : "string",
            "enum" : [ "low", "medium", "high" ]
          },
          "allowPaidRentals" : {
            "type" : "boolean"
          },
          "maximumRentalPrice" : {
            "maximum" : 1000.0,
            "exclusiveMaximum" : false,
            "minimum" : 0.0,
            "exclusiveMinimum" : false,
            "type" : "number"
          },
          "allowPhysicalMedia" : {
            "type" : "boolean"
          },
          "weeklyViewingMinutesOverride" : {
            "maximum" : 10080,
            "minimum" : 0,
            "type" : "integer",
            "format" : "int32"
          }
        },
        "description" : "Partial update. Omitted fields are left unchanged."
      },
      "UserSettingsResponse" : {
        "required" : [ "allowPaidRentals", "allowPhysicalMedia", "defaultAccessPolicy", "defaultCommitmentPreference", "defaultNoveltyPreference", "updatedAt" ],
        "type" : "object",
        "properties" : {
          "maximumMonthlyBudget" : {
            "type" : "number"
          },
          "maximumActiveServices" : {
            "type" : "integer",
            "format" : "int32"
          },
          "maximumMonthlySwitches" : {
            "type" : "integer",
            "format" : "int32"
          },
          "defaultAvailableMinutes" : {
            "type" : "integer",
            "format" : "int32"
          },
          "defaultAccessPolicy" : {
            "type" : "string"
          },
          "defaultNoveltyPreference" : {
            "type" : "number"
          },
          "defaultCommitmentPreference" : {
            "type" : "string"
          },
          "allowPaidRentals" : {
            "type" : "boolean"
          },
          "maximumRentalPrice" : {
            "type" : "number"
          },
          "allowPhysicalMedia" : {
            "type" : "boolean"
          },
          "weeklyViewingMinutesOverride" : {
            "type" : "integer",
            "format" : "int32"
          },
          "updatedAt" : {
            "type" : "string",
            "format" : "date-time"
          }
        }
      },
      "UpdateSubscriptionRequest" : {
        "required" : [ "clearNotes", "clearRenewsOn" ],
        "type" : "object",
        "properties" : {
          "status" : {
            "type" : "string",
            "enum" : [ "active", "paused", "cancelled", "trial", "lapsed" ]
          },
          "renewsOn" : {
            "type" : "string",
            "format" : "date"
          },
          "clearRenewsOn" : {
            "type" : "boolean",
            "description" : "Set true to remove an existing renewal date."
          },
          "autoRenews" : {
            "type" : "boolean"
          },
          "cannotCancel" : {
            "type" : "boolean"
          },
          "notes" : {
            "maxLength" : 2000,
            "minLength" : 0,
            "type" : "string"
          },
          "clearNotes" : {
            "type" : "boolean",
            "description" : "Set true to remove existing notes."
          }
        }
      },
      "UpdateAlertRequest" : {
        "required" : [ "status" ],
        "type" : "object",
        "properties" : {
          "status" : {
            "type" : "string",
            "enum" : [ "read", "dismissed" ]
          }
        }
      },
      "WatchlistResponse" : {
        "required" : [ "id", "items", "name" ],
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "items" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/WatchlistItemResponse"
            }
          }
        }
      },
      "CoverageResponse" : {
        "required" : [ "attribution", "consideredTitles", "providers", "regionCode", "unknownTitles" ],
        "type" : "object",
        "properties" : {
          "regionCode" : {
            "type" : "string"
          },
          "consideredTitles" : {
            "type" : "integer",
            "description" : "Outstanding items with availability data. The denominator of every share below.",
            "format" : "int32"
          },
          "unknownTitles" : {
            "type" : "integer",
            "description" : "Outstanding items nobody has checked yet. Reported, never scored.",
            "format" : "int32"
          },
          "providers" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/ProviderCoverageResponse"
            }
          },
          "attribution" : {
            "type" : "string"
          }
        },
        "description" : "Which service covers the largest priority-weighted share of the outstanding watchlist. Titles whose availability has never been checked are excluded from the calculation and reported in unknownTitles, so a stale catalogue does not silently depress every score."
      },
      "CoveredTitleResponse" : {
        "required" : [ "priority", "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "priority" : {
            "type" : "integer",
            "format" : "int32"
          }
        }
      },
      "ProviderCoverageResponse" : {
        "required" : [ "name", "providerId", "slug", "titleCount", "titles", "weightedShare" ],
        "type" : "object",
        "properties" : {
          "providerId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "slug" : {
            "type" : "string"
          },
          "logoUrl" : {
            "type" : "string"
          },
          "titleCount" : {
            "type" : "integer",
            "format" : "int32"
          },
          "weightedShare" : {
            "type" : "number",
            "description" : "Share of the total priority weight this service covers, 0 to 1.",
            "format" : "double"
          },
          "titles" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/CoveredTitleResponse"
            }
          }
        }
      },
      "BlockedTitlesResponse" : {
        "required" : [ "blocked" ],
        "type" : "object",
        "properties" : {
          "blocked" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/BlockedTitleResponse"
            }
          }
        }
      },
      "DiagnosisResponse" : {
        "required" : [ "headline", "reasons" ],
        "type" : "object",
        "properties" : {
          "headline" : {
            "type" : "string"
          },
          "reasons" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/RejectionResponse"
            }
          }
        },
        "description" : "Present only when nothing fit."
      },
      "PickResponse" : {
        "required" : [ "availableOn", "exploration", "mediaType", "name", "perEpisode", "position", "reasons", "score", "titleId" ],
        "type" : "object",
        "properties" : {
          "position" : {
            "type" : "integer",
            "description" : "1 is the pick, 2 and 3 are backups.",
            "format" : "int32"
          },
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "mediaType" : {
            "type" : "string"
          },
          "posterUrl" : {
            "type" : "string"
          },
          "watchMinutes" : {
            "type" : "integer",
            "description" : "The whole commitment: a film's runtime, or every episode of a series added up. Shown so somebody can see what they are taking on. Not what the time filter used.",
            "format" : "int32"
          },
          "sessionMinutes" : {
            "type" : "integer",
            "description" : "One sitting: a film, or a typical episode. This is what the time budget was measured against, because a long series is watched in increments rather than in one go.",
            "format" : "int32"
          },
          "perEpisode" : {
            "type" : "boolean",
            "description" : "True when sessionMinutes describes an episode rather than the whole title."
          },
          "availableOn" : {
            "type" : "array",
            "description" : "Where it can be watched, under the access policy that was applied.",
            "items" : {
              "type" : "string",
              "description" : "Where it can be watched, under the access policy that was applied."
            }
          },
          "score" : {
            "type" : "number",
            "description" : "0 to 1, after renormalising over the features this title actually has.",
            "format" : "double"
          },
          "reasons" : {
            "type" : "array",
            "description" : "Why it was chosen, largest contribution first. Derived from the scored features themselves — never generated prose.",
            "items" : {
              "$ref" : "#/components/schemas/ReasonResponse"
            }
          },
          "exploration" : {
            "type" : "boolean",
            "description" : "True when this slot was filled by exploration rather than by rank. Surfaced rather than hidden: a suggestion that is deliberately a wildcard should say so."
          }
        }
      },
      "ReasonResponse" : {
        "required" : [ "reason", "share" ],
        "type" : "object",
        "properties" : {
          "reason" : {
            "type" : "string"
          },
          "share" : {
            "type" : "number",
            "description" : "How much of the score this accounted for, 0 to 1.",
            "format" : "double"
          }
        },
        "description" : "Why it was chosen, largest contribution first. Derived from the scored features themselves — never generated prose."
      },
      "RejectionResponse" : {
        "required" : [ "count", "explanation", "reason" ],
        "type" : "object",
        "properties" : {
          "reason" : {
            "type" : "string"
          },
          "explanation" : {
            "type" : "string"
          },
          "count" : {
            "type" : "integer",
            "format" : "int32"
          }
        }
      },
      "TonightResponse" : {
        "required" : [ "candidateCount", "eligibleCount", "picks", "requestId" ],
        "type" : "object",
        "properties" : {
          "requestId" : {
            "type" : "string",
            "description" : "The decision this answer came from. Pass it back when accepting a pick, so the acceptance attaches to the exact item that was offered rather than merely to a title.",
            "format" : "uuid"
          },
          "picks" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/PickResponse"
            }
          },
          "diagnosis" : {
            "$ref" : "#/components/schemas/DiagnosisResponse"
          },
          "candidateCount" : {
            "type" : "integer",
            "description" : "How many watchlist items were considered before filtering.",
            "format" : "int32"
          },
          "eligibleCount" : {
            "type" : "integer",
            "description" : "How many survived the hard filters.",
            "format" : "int32"
          }
        },
        "description" : "Tonight's answer. An empty picks list with a populated diagnosis is a valid, successful response: it means the constraints excluded everything, which is information rather than a failure."
      },
      "AvailabilityOfferResponse" : {
        "required" : [ "accessType", "confidence", "providerName", "providerSlug", "providerType", "source", "verifiedAt" ],
        "type" : "object",
        "properties" : {
          "providerName" : {
            "type" : "string"
          },
          "providerSlug" : {
            "type" : "string"
          },
          "providerType" : {
            "type" : "string",
            "description" : "subscription, free, transactional, library or linear."
          },
          "providerLogoUrl" : {
            "type" : "string"
          },
          "accessType" : {
            "type" : "string",
            "description" : "subscription, free, ads, rent, buy or library."
          },
          "price" : {
            "type" : "number"
          },
          "currency" : {
            "type" : "string"
          },
          "deepLink" : {
            "type" : "string"
          },
          "source" : {
            "type" : "string"
          },
          "verifiedAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "confidence" : {
            "type" : "number",
            "description" : "0 to 1. Below 1 means part of the upstream response could not be mapped."
          }
        }
      },
      "AvailabilityResponse" : {
        "required" : [ "attribution", "offers", "regionCode", "stale" ],
        "type" : "object",
        "properties" : {
          "regionCode" : {
            "type" : "string"
          },
          "offers" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/AvailabilityOfferResponse"
            }
          },
          "lastVerifiedAt" : {
            "type" : "string",
            "description" : "When the freshest entry was last checked. Null when nothing is known.",
            "format" : "date-time"
          },
          "stale" : {
            "type" : "boolean",
            "description" : "True when nothing here has been checked recently enough to rely on. Clients should label the data as stale rather than hide it -- suppressing prices is the documented degraded behaviour, not suppressing the title."
          },
          "attribution" : {
            "type" : "string"
          }
        },
        "description" : "Where a title can be watched. Every entry carries its source and when it was last verified: availability data is imperfect, particularly for smaller Canadian services, and a decision tool that hides that is worse than one that admits it."
      },
      "DiscoverResultResponse" : {
        "required" : [ "externalId", "mediaType", "name" ],
        "type" : "object",
        "properties" : {
          "externalId" : {
            "type" : "string"
          },
          "mediaType" : {
            "type" : "string"
          },
          "name" : {
            "type" : "string"
          },
          "releaseDate" : {
            "type" : "string",
            "format" : "date"
          },
          "overview" : {
            "type" : "string"
          },
          "posterUrl" : {
            "type" : "string"
          }
        },
        "description" : "A result from TMDB, which Plotted may not have ingested yet. Add it with POST /api/v1/titles to get a Plotted identifier."
      },
      "SubscriptionListResponse" : {
        "required" : [ "countedSubscriptions", "currency", "monthlyTotal", "subscriptions" ],
        "type" : "object",
        "properties" : {
          "subscriptions" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/SubscriptionResponse"
            }
          },
          "monthlyTotal" : {
            "type" : "number",
            "description" : "Total monthly cost of active and trial subscriptions only."
          },
          "currency" : {
            "type" : "string"
          },
          "countedSubscriptions" : {
            "type" : "integer",
            "format" : "int32"
          }
        }
      },
      "ProviderListResponse" : {
        "required" : [ "providers" ],
        "type" : "object",
        "properties" : {
          "providers" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/ProviderResponse"
            }
          }
        }
      },
      "ProviderResponse" : {
        "required" : [ "id", "name", "slug", "type" ],
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "slug" : {
            "type" : "string"
          },
          "type" : {
            "type" : "string"
          },
          "logoUrl" : {
            "type" : "string"
          }
        },
        "description" : "A streaming service. No price: the user supplies what they pay."
      },
      "ExcludedResponse" : {
        "required" : [ "freeToWatch", "neverChecked", "unpricedService" ],
        "type" : "object",
        "properties" : {
          "freeToWatch" : {
            "type" : "array",
            "description" : "Watchable free or ad-supported, so no subscription decision turns on them.",
            "items" : {
              "$ref" : "#/components/schemas/ExcludedTitleResponse"
            }
          },
          "neverChecked" : {
            "type" : "array",
            "description" : "Availability has never been checked. Excluded from the denominator, never scored as uncovered.",
            "items" : {
              "$ref" : "#/components/schemas/ExcludedTitleResponse"
            }
          },
          "unpricedService" : {
            "type" : "array",
            "description" : "Only on services with no established price. Guessing one would put invented money in the objective.",
            "items" : {
              "$ref" : "#/components/schemas/ExcludedTitleResponse"
            }
          }
        },
        "description" : "Watchlist items the optimiser was never shown. Reported rather than dropped: a title missing because Plotted has never checked it is a completely different fact from one no plan could afford."
      },
      "ExcludedTitleResponse" : {
        "required" : [ "name", "providerNames", "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "providerNames" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            }
          }
        },
        "description" : "Only on services with no established price. Guessing one would put invented money in the objective."
      },
      "MonthResponse" : {
        "required" : [ "month", "monthlyCents", "started", "stopped", "subscribed" ],
        "type" : "object",
        "properties" : {
          "month" : {
            "type" : "integer",
            "description" : "0 is this month.",
            "format" : "int32"
          },
          "monthlyCents" : {
            "type" : "integer",
            "format" : "int64"
          },
          "subscribed" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/ServiceRefResponse"
            }
          },
          "started" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/ServiceRefResponse"
            }
          },
          "stopped" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/ServiceRefResponse"
            }
          }
        }
      },
      "ObjectiveResponse" : {
        "required" : [ "costFraction", "coverage", "switchFraction", "weighted" ],
        "type" : "object",
        "properties" : {
          "coverage" : {
            "type" : "number",
            "description" : "Share of watchlist priority points the plan delivers.",
            "format" : "double"
          },
          "costFraction" : {
            "type" : "number",
            "description" : "Spend as a fraction of subscribing to everything for the whole horizon.",
            "format" : "double"
          },
          "switchFraction" : {
            "type" : "number",
            "description" : "Switches as a fraction of the maximum possible.",
            "format" : "double"
          },
          "weighted" : {
            "type" : "number",
            "format" : "double"
          }
        },
        "description" : "Each component on 0..1, recomputed exactly from the chosen plan rather than read back out of the solver — so these are the numbers, not what the model believed after its coefficients were made integral."
      },
      "PlanCoveredTitleResponse" : {
        "required" : [ "month", "name", "providerId", "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "month" : {
            "type" : "integer",
            "description" : "The first month the plan makes it reachable.",
            "format" : "int32"
          },
          "providerId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "providerName" : {
            "type" : "string"
          }
        }
      },
      "PlanDiagnosisResponse" : {
        "required" : [ "explanation" ],
        "type" : "object",
        "properties" : {
          "explanation" : {
            "type" : "string"
          },
          "bindingConstraint" : {
            "type" : "string",
            "description" : "Which limit made it impossible, when one can be identified."
          }
        },
        "description" : "Present only when no plan was produced."
      },
      "PlanResponse" : {
        "required" : [ "covered", "excluded", "horizonMonths", "months", "sensitivity", "status", "uncovered", "violations" ],
        "type" : "object",
        "properties" : {
          "status" : {
            "type" : "string"
          },
          "horizonMonths" : {
            "type" : "integer",
            "format" : "int32"
          },
          "months" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/MonthResponse"
            }
          },
          "diagnosis" : {
            "$ref" : "#/components/schemas/PlanDiagnosisResponse"
          },
          "objective" : {
            "$ref" : "#/components/schemas/ObjectiveResponse"
          },
          "totalCents" : {
            "type" : "integer",
            "description" : "Total spend across the whole horizon, in cents.",
            "format" : "int64"
          },
          "covered" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/PlanCoveredTitleResponse"
            }
          },
          "uncovered" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/UncoveredTitleResponse"
            }
          },
          "sensitivity" : {
            "type" : "array",
            "description" : "What relaxing each binding limit by one unit would buy. Empty when nothing is binding.",
            "items" : {
              "$ref" : "#/components/schemas/SensitivityResponse"
            }
          },
          "excluded" : {
            "$ref" : "#/components/schemas/ExcludedResponse"
          },
          "violations" : {
            "type" : "array",
            "description" : "Problems an independent reimplementation of the rules found in the solver's own answer. Always empty in a healthy system: non-empty means the model and the rules disagree, which is a defect in the model. Surfaced rather than hidden, because a wrong plan you can see beats one you cannot.",
            "items" : {
              "type" : "string",
              "description" : "Problems an independent reimplementation of the rules found in the solver's own answer. Always empty in a healthy system: non-empty means the model and the rules disagree, which is a defect in the model. Surfaced rather than hidden, because a wrong plan you can see beats one you cannot."
            }
          },
          "solveMillis" : {
            "type" : "integer",
            "format" : "int64"
          }
        },
        "description" : "A subscription plan month by month. `months` is empty with a populated `diagnosis` when no plan satisfies the limits that were set, or when there is nothing on the watchlist to plan against — both are successful responses carrying information, not failures."
      },
      "SensitivityResponse" : {
        "required" : [ "constraint", "coverageDelta", "monthlyCentsDelta", "relaxedBy" ],
        "type" : "object",
        "properties" : {
          "constraint" : {
            "type" : "string"
          },
          "relaxedBy" : {
            "type" : "string"
          },
          "coverageDelta" : {
            "type" : "number",
            "description" : "Extra share of the watchlist this would buy, 0..1.",
            "format" : "double"
          },
          "monthlyCentsDelta" : {
            "type" : "integer",
            "description" : "Change in peak monthly spend, in cents. Positive means it costs more.",
            "format" : "int64"
          }
        },
        "description" : "What relaxing each binding limit by one unit would buy. Empty when nothing is binding."
      },
      "ServiceRefResponse" : {
        "required" : [ "name", "providerId" ],
        "type" : "object",
        "properties" : {
          "providerId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          }
        }
      },
      "UncoveredTitleResponse" : {
        "required" : [ "availableOn", "name", "priorityPoints", "reason", "titleId" ],
        "type" : "object",
        "properties" : {
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string"
          },
          "priorityPoints" : {
            "type" : "integer",
            "format" : "int32"
          },
          "reason" : {
            "type" : "string",
            "description" : "not_carried — no priced service has it. not_chosen — the plan could not fit it."
          },
          "availableOn" : {
            "type" : "array",
            "items" : {
              "type" : "string"
            }
          }
        }
      },
      "AxisOpinionResponse" : {
        "required" : [ "axis", "label", "negative", "positive", "sentence", "standardError", "stated", "verdict", "weight" ],
        "type" : "object",
        "properties" : {
          "axis" : {
            "type" : "string"
          },
          "label" : {
            "type" : "string"
          },
          "positive" : {
            "type" : "string"
          },
          "negative" : {
            "type" : "string"
          },
          "weight" : {
            "type" : "number",
            "format" : "double"
          },
          "standardError" : {
            "type" : "number",
            "description" : "Posterior standard deviation. What separates a finding from an unasked question.",
            "format" : "double"
          },
          "verdict" : {
            "type" : "string",
            "enum" : [ "LIKES", "DISLIKES", "NO_PREFERENCE", "NOT_ASKED" ]
          },
          "stated" : {
            "type" : "boolean"
          },
          "sentence" : {
            "type" : "string"
          }
        },
        "description" : "One axis. NO_PREFERENCE and NOT_ASKED are both ways of saying we do not know, and they are not interchangeable: the first means we asked and you were balanced, the second that the ladder never contrasted this axis, so the weight is the population's rather than yours."
      },
      "PreferenceProfileResponse" : {
        "required" : [ "axes", "converged", "informative", "observations" ],
        "type" : "object",
        "properties" : {
          "observations" : {
            "type" : "integer",
            "format" : "int32"
          },
          "converged" : {
            "type" : "boolean"
          },
          "informative" : {
            "type" : "boolean",
            "description" : "Whether the profile has anything it can defend saying. When false, ranking ignores it."
          },
          "axes" : {
            "type" : "array",
            "description" : "Every axis, including the ones with no verdict. What was not found is part of the answer.",
            "items" : {
              "$ref" : "#/components/schemas/AxisOpinionResponse"
            }
          }
        },
        "description" : "A fitted taste profile. Null until at least one question has been answered — a profile fitted from no answers is the population's rather than yours, and reporting it would present the prior back as a discovery."
      },
      "CompletionRateResponse" : {
        "required" : [ "completed", "judged", "tooRecentToJudge" ],
        "type" : "object",
        "properties" : {
          "rate" : {
            "type" : "number",
            "description" : "0 to 1, or null when nothing has had the chance to be finished yet.",
            "format" : "double"
          },
          "completed" : {
            "type" : "integer",
            "format" : "int32"
          },
          "judged" : {
            "type" : "integer",
            "format" : "int32"
          },
          "tooRecentToJudge" : {
            "type" : "integer",
            "format" : "int32"
          }
        },
        "description" : "Of the picks you accepted, how many you finished. Acceptances newer than fourteen days are held back rather than counted as failures, which would make the rate climb on its own as the log aged."
      },
      "DecisionLatencyResponse" : {
        "required" : [ "excludedAsStale", "sampleSize" ],
        "type" : "object",
        "properties" : {
          "medianSeconds" : {
            "type" : "integer",
            "format" : "int64"
          },
          "fastestSeconds" : {
            "type" : "integer",
            "format" : "int64"
          },
          "slowestSeconds" : {
            "type" : "integer",
            "format" : "int64"
          },
          "sampleSize" : {
            "type" : "integer",
            "format" : "int32"
          },
          "excludedAsStale" : {
            "type" : "integer",
            "description" : "Acceptances more than four hours after the request. A later session, not a slow decision.",
            "format" : "int32"
          }
        },
        "description" : "How long between being shown picks and choosing one. The median, because wall-clock has an unbounded tail. Null until at least one acceptance qualifies — a latency computed from nothing is not zero."
      },
      "EndCreditsResponse" : {
        "required" : [ "acceptedAndCompleted", "decisionLatency", "nothingFitCount", "recommendationsServed" ],
        "type" : "object",
        "properties" : {
          "decisionLatency" : {
            "$ref" : "#/components/schemas/DecisionLatencyResponse"
          },
          "acceptedAndCompleted" : {
            "$ref" : "#/components/schemas/CompletionRateResponse"
          },
          "recommendationsServed" : {
            "type" : "integer",
            "format" : "int32"
          },
          "nothingFitCount" : {
            "type" : "integer",
            "description" : "Requests that returned nothing. Reported beside the rest: refusing is a feature.",
            "format" : "int32"
          }
        },
        "description" : "The two metrics that carry Plotted's argument: does it save time, and did you watch the thing. Everything else is decoration."
      },
      "AlertResponse" : {
        "required" : [ "alertType", "createdAt", "id", "message", "severity" ],
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "alertType" : {
            "type" : "string",
            "description" : "For example availability.left."
          },
          "severity" : {
            "type" : "string",
            "enum" : [ "info", "warning", "urgent" ]
          },
          "titleId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "message" : {
            "type" : "string"
          },
          "createdAt" : {
            "type" : "string",
            "format" : "date-time"
          }
        },
        "description" : "Something Plotted needs to tell you."
      },
      "AlertsResponse" : {
        "required" : [ "alerts" ],
        "type" : "object",
        "properties" : {
          "alerts" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/AlertResponse"
            }
          }
        }
      }
    },
    "securitySchemes" : {
      "bearerAuth" : {
        "type" : "http",
        "description" : "Short-lived access token issued by /api/v1/auth/login.",
        "scheme" : "bearer",
        "bearerFormat" : "JWT"
      }
    }
  }
}
